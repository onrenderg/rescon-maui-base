<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ResillentConstruction.ProfilePage"
             xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls" 
             ios:Page.UseSafeArea="true">

    <NavigationPage.TitleView  >
        <StackLayout x:Name="Custom_Navigation" Orientation="Horizontal" HorizontalOptions="FillAndExpand" VerticalOptions="CenterAndExpand" >
            <Image Source="ic_himachallogo.png"  HeightRequest="40"  VerticalOptions="CenterAndExpand"/>
            <StackLayout  Orientation="Vertical" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" Spacing="0">
                <Label  x:Name="lbl_navigation_header" TextColor="White" FontSize="20" FontAttributes="Bold" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
            </StackLayout>
            
        </StackLayout>
    </NavigationPage.TitleView>
    <ContentPage.ToolbarItems>
        <ToolbarItem  IconImageSource="language_ico.png" Clicked="ToolbarItem_Clicked"/>
    </ContentPage.ToolbarItems>

    <Grid>
        <StackLayout Spacing="3" Padding="5">

            <Label x:Name="lbl_user_header1" Text="Profile" Padding="3"  FontSize="18" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" Margin="5" FontAttributes="Bold" TextColor="White" BackgroundColor="Navy" />
            <Label x:Name="lbl_mandatory" Text="*Mandatory Fields" HorizontalOptions="FillAndExpand" VerticalOptions="Center" FontSize="14"  FontAttributes="Bold"   HorizontalTextAlignment="Center"/>
            <Label x:Name="lbl_gpsdistrict" Text="District Name" HorizontalOptions="FillAndExpand" VerticalOptions="Center" FontSize="18"  FontAttributes="Bold"   HorizontalTextAlignment="Center"/>

            <ScrollView VerticalOptions ="FillAndExpand">
                <Frame CornerRadius="10"  BorderColor="Navy" BackgroundColor="Transparent" Margin="5" Padding="7">
                    <StackLayout Orientation="Vertical" >

                        <Label x:Name="lbl_name" Text="Name *" FontSize="20"   FontAttributes="Bold"  />
                        <Entry Placeholder="Enter Name" x:Name="entry_name" FontSize="20" MaxLength="50" Keyboard="Text"/>

                        <Label  x:Name="lbl_mobile" Text="Mobile *" FontSize="20"   FontAttributes="Bold"  />
                        <Entry  x:Name="entry_mobile" FontSize="20" MaxLength="10" Keyboard="Numeric"/>

                        <Label  x:Name="lbl_email" Text="Email *" FontSize="20"   FontAttributes="Bold"  />
                        <Entry  x:Name="entry_email" FontSize="20" MaxLength="50" Keyboard="Email"/>

                        <Label x:Name="lbl_District" Text="District *" FontSize="20"   FontAttributes="Bold"   />
                        <Grid ColumnDefinitions="*,Auto">
                            <Picker Title="Select District" x:Name="Picker_District" SelectedIndexChanged="Picker_District_SelectedIndexChanged" Grid.Column="0" FontSize="20" />
                            <Button x:Name="Btn_getdistrict"
                            ImageSource="location_searching.png"
                            Clicked="Btn_getdistrict_Clicked"
                            BackgroundColor="Navy"
                            TextColor="White"
                            Grid.Column="1"
                            IsVisible="False"
                            ContentLayout="Left,10" />

                        </Grid>

                        <Label  x:Name="lbl_fallinzone" Text="You fall in Zone : " FontSize="20" HorizontalOptions="Center"   FontAttributes="Bold"  />

                        <Label x:Name="lbl_place" Text="Enter Place Of construction *" FontSize="20"   FontAttributes="Bold"   />
                        <Entry x:Name="entry_place" Placeholder="Enter Place Of construction"  FontSize="20" MaxLength="50" Keyboard="Text"/>

                    </StackLayout>

                </Frame>
            </ScrollView>

            <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand" Spacing="10">
                <Button HorizontalOptions="FillAndExpand"  Text="Cancel" x:Name="Btn_cancel" Clicked="Btn_cancel_Clicked" BackgroundColor="Silver" TextColor="Black"  FontSize="18" FontAttributes="Bold"/>
                <Button HorizontalOptions="FillAndExpand"  Text="Save" x:Name="Btn_save" Clicked="Btn_save_Clicked" BackgroundColor="Navy" TextColor="White" FontSize="18" FontAttributes="Bold"/>
            </StackLayout>
        </StackLayout>

        <ContentView x:Name="Loading_activity" BackgroundColor="#C0808080" Padding="20, 20" IsVisible="false" AbsoluteLayout.LayoutBounds="0, 0, 1, 1" AbsoluteLayout.LayoutFlags="All">
            <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
                <Frame  HorizontalOptions="FillAndExpand">
                    <StackLayout Orientation="Horizontal" Margin="6" HorizontalOptions="FillAndExpand" Spacing="10" Padding="4,0">
                        <ActivityIndicator IsVisible="true" IsRunning="true" Margin="5"/>
                        <Label x:Name="lbl_PleaseWait"
                               Text="Please Wait ..."
                               TextColor="Black"
                               FontAttributes="Bold"
                               HorizontalOptions="FillAndExpand"
                               VerticalTextAlignment="Center"
                               LineBreakMode="WordWrap"
                               MaxLines="3"/>
                    </StackLayout>
                </Frame>
            </StackLayout>
        </ContentView>
    </Grid>
</ContentPage>